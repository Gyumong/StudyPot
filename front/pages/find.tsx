import React, { ReactElement } from "react";
import AuthTemplate from "@layouts/auth";

const find = (): ReactElement => {
  return (
    <AuthTemplate>
    </AuthTemplate>
  );
};

export default find;
